name: Cron weekly reports

on:
  schedule:
    - cron: "0 13 * * 1"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping weekly reports
        run: curl -fsS -X GET -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" https://alphaa.app/api/cron/weekly
