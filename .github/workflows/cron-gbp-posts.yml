name: Cron GBP posts

on:
  schedule:
    - cron: "0 13 * * 1,4"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping GBP posts
        run: curl -fsS -X GET -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" https://alphaa.app/api/cron/gbp-posts
